{
  "name": "holi-app",
  "version": "0.1.0",
  "description": "A knowledge management app.",
  "repository": {
    "type": "git",
    "url": "https://github.com/csirvente/holi.git"
  },
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/csirvente/holi/issues"
  },
  "homepage": "https://github.com/csirvente/holi#readme",
  "engines": {
    "npm": "5.5.1",
    "node": "8.9.1"
  },
  "scripts": {
    "start": "concurrently --kill-others \"npm start --prefix api\" \".\\scripts\\wait.sh && npm start --prefix ui\"",
    "postinstall": "npm install --prefix api && npm install --prefix ui",
    "build": "npm run build --prefix api && npm run build --prefix ui",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "babel-eslint": "^8.0.3",
    "concurrently": "^3.5.1",
    "normalize-url": "^4.5.1"
  }
}
